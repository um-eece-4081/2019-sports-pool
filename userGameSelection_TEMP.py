#Temporary file waiting for UI Team to finish UserGameSelection...

def score(self):
    if self.selected_team == self.game.winner(): #winner() function will reside in Game
        return 1
    else
        return 0
